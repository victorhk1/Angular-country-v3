import { SortContinentPipe } from './sort-continent.pipe';

describe('SortContinentPipe', () => {
  it('create an instance', () => {
    const pipe = new SortContinentPipe();
    expect(pipe).toBeTruthy();
  });
});
